# PID Simulator

In this iPython notebook, we simulate a system and controller.

## Installation

You will need to install (iPython)[http://ipython.org/] as well as a few python libraries:

```
pip3 install numpy
pip3 install scipy
pip3 install matplotlib

pip3 install control
```